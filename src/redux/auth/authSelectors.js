export const selectedIsLoggedIn = state => state.auth.isLoggedIn;

export const selectedUser = state => state.auth.user;

export const selectedIsFetchingCurrentUser = state => state.auth.isFetchingCurrentUser;